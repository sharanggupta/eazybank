# Card service - Dev environment - Application configuration
image:
  tag: latest

app:
  spring:
    profiles: dev
  build:
    version: 0.0.1
  datasource:
    username: postgres
    password: "__DB_PASSWORD__"
  support:
    contactName: Card Dev Support
    contactEmail: dev-support@card.service.local
    contactNumber: "+1-555-0002"
    message: Welcome to EazyBank Card Service (Dev)
  observability:
    enabled: true
    samplingRate: "1.0"
    otlpEndpoint: "http://otel-collector:4318"

postgresql:
  credentials:
    username: postgres
    password: "__DB_PASSWORD__"
